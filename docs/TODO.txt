TODO
========================================================
1/
gestion des cas avec directory en entree 
-> noms de fichiers multi=nom fichier input dans repertoire outArg -> ok pour MEd2ODV
-> modifier GUI: demander repertoire si entrée=répertoire

2/
gestion cas d'erreur:
rep sortie : pas de droits d'ecriture

A VOIR
=========================================================
1/
BODC: telechargement pendant export reporte affichage de la log 
-> on laisse comme ça, car l'appli est livrée avec les fichiers BODC, donc ne doit pas se produire
on pourrait tester l'existence et le contenu du rep BODC pour afficher un message d'attente si besoin

2/
restriction sur MEdnon SDN: impossible de demander un split avec une liste de local_cdi_id (cdi non trouvés)
-> on pourrait essayer de detecter les CDI en les recomposant (co on fait pour ecrire en sortie)

ERREURS
=========================================================
Med2Med: si le CDI n'est pas present, on ecrit qd mm le header -> correction a verifier








LIVRABLE
==============
FERMER les projets dependances de Octopus dans le workspace eclipse (sinon ils ne sont pas inclus dans le path du build!)
mvn clean install
build ant

zipper un repertoire contenant
octopus/build/dist/octopus.jar
octopus/build/dist/libs/* 
octopus/resources
